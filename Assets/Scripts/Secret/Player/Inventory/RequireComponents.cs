using UnityEngine;

namespace Secret.Player.Inventory
{
    [RequireComponent(typeof(ChooserWeapon))]
    [RequireComponent(typeof(Container))]
    [RequireComponent(typeof(StartWeaponAdder))]

    public class RequireComponents : MonoBehaviour { }
}