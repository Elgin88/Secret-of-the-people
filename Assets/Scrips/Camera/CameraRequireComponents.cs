using UnityEngine;

namespace Shooter.Camera
{
    [RequireComponent(typeof(Camera))]
    [RequireComponent(typeof(CameraSetterPosition))]

    public class CameraRequireComponents : MonoBehaviour
    {
    }
}